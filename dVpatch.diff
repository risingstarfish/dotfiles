diff --git a/FATCATMain/AFPchain.C b/FATCATMain/AFPchain.C
index 7120388..a32fde5 100644
--- a/FATCATMain/AFPchain.C
+++ b/FATCATMain/AFPchain.C
@@ -25,30 +25,31 @@ int colorlist[] = {255,0,0,  205,0,205,  255,255,0, 0,255,0,  0,0,255,  0,255,25
 //------------------------------------------------------------------
 //Constructors: assign two proteins and extract their AFPs
 //------------------------------------------------------------------
-AFPCHAIN::AFPCHAIN(char *file1, char *file2, char *output)
+AFPCHAIN::AFPCHAIN(char *file1, char *file2, char *output, PdbErr *err, int *lineNo, char *errFile_out)
 {
-	AFPCHAIN(file1, 0, 100000, file2, 0, 100000, 0, output); 
-		          //big enough number to cover the whole length of possible longest protein
+	Construct(file1, 0, 100000, file2, 0, 100000, 0, output, err, lineNo, errFile_out); 
 }
 
-//------------------------------------------------------------------
-//Constructors: assign two proteins and extract their AFPs
-//------------------------------------------------------------------
-AFPCHAIN::AFPCHAIN(char *file1, char *file2, int sps, char *output)
+AFPCHAIN::AFPCHAIN(char *file1, char *file2, int sps, char *output, PdbErr *err, int *lineNo, char *errFile_out)
 {
-	AFPCHAIN(file1, 0, 100000, file2, 0, 100000, sps, output); 
-		          //big enough number to cover the whole length of possible longest protein
+	Construct(file1, 0, 100000, file2, 0, 100000, sps, output, err, lineNo, errFile_out); 
 }
 
-//------------------------------------------------------------------
-//Constructors: assign two proteins and extract their AFPs
-//------------------------------------------------------------------
-AFPCHAIN::AFPCHAIN(char *file1, int s1, int l1, char *file2, int s2, int l2, int sps, char *output)
+AFPCHAIN::AFPCHAIN(char *file1, int s1, int l1, char *file2, int s2, int l2, int sps, char *output, PdbErr *err, int *lineNo, char *errFile_out)
+{
+	Construct(file1, s1, l1, file2, s2, l2, sps, output, err, lineNo, errFile_out);
+}
+
+void AFPCHAIN::Construct(char *file1, int s1, int l1, char *file2, int s2, int l2, int sps, char *output, PdbErr *err, int *lineNo, char *errFile_out)
 {
 	bgtime = time(NULL);
 	Initial();
 	SetParameters();
 	sparse = sps;
+	errCode = PDB_OK;
+	errLine = 0;
+	errFile[0] = '\0';
+
 	int	i, p1, p2;
 	for(i = strlen(file1) - 1; i >= 0; i --)	{
 		if(file1[i] == '/')	break;	
@@ -61,17 +62,58 @@ AFPCHAIN::AFPCHAIN(char *file1, int s1, int l1, char *file2, int s2, int l2, int
 	strcpy(pro1Name, file1 + p1);
 	strcpy(pro2Name, file2 + p2);
 
-	pro1 = new PROT(file1, s1, l1);
+	PdbErr e1 = PDB_OK, e2 = PDB_OK;
+	int l1_err = 0, l2_err = 0;
+
+	pro1 = new PROT(file1, s1, l1, &e1, &l1_err);
+	if(e1 != PDB_OK) {
+		errCode = e1;
+		errLine = l1_err;
+		strcpy(errFile, file1);
+		if (err) *err = errCode;
+		if (lineNo) *lineNo = errLine;
+		if (errFile_out) strcpy(errFile_out, file1);
+		if (err == NULL) {
+			if (errLine > 0) fprintf(stderr, "Error: %s at line %d in %s\n", pdbErrStr(errCode), errLine, errFile);
+			else fprintf(stderr, "Error: %s in %s\n", pdbErrStr(errCode), errFile);
+			exit(1);
+		}
+		return;
+	}
 	pro1Len = pro1->length;
 	if(debug)	printf("protein 1 %s len %d\n", pro1Name, pro1Len);
 
-	pro2 = new PROT(file2, s2, l2);
+	pro2 = new PROT(file2, s2, l2, &e2, &l2_err);
+	if(e2 != PDB_OK) {
+		errCode = e2;
+		errLine = l2_err;
+		strcpy(errFile, file2);
+		if (err) *err = errCode;
+		if (lineNo) *lineNo = errLine;
+		if (errFile_out) strcpy(errFile_out, file2);
+		if (err == NULL) {
+			if (errLine > 0) fprintf(stderr, "Error: %s at line %d in %s\n", pdbErrStr(errCode), errLine, errFile);
+			else fprintf(stderr, "Error: %s in %s\n", pdbErrStr(errCode), errFile);
+			exit(1);
+		}
+		return;
+	}
 	pro2Len = pro2->length;
 	if(debug)	printf("protein 2 %s len %d\n", pro2Name, pro2Len);
 
 	if(pro1Len <= 0 || pro2Len <= 0)	{
-		printf("Wrong pdb read %d %d\n", pro1Len, pro2Len);
-		exit(1);
+		errCode = PDB_TRUNCATED;
+		errLine = 0;
+		char *bad = (pro1Len <= 0) ? file1 : file2;
+		strcpy(errFile, bad);
+		if (err) *err = errCode;
+		if (lineNo) *lineNo = 0;
+		if (errFile_out) strcpy(errFile_out, bad);
+		if (err == NULL) {
+			fprintf(stderr, "Error: %s in %s\n", pdbErrStr(errCode), errFile);
+			exit(1);
+		}
+		return;
 	}
 
 	if(pro1Len < pro2Len)	minLen = pro1Len;
@@ -93,6 +135,9 @@ AFPCHAIN::AFPCHAIN(char *file1, int s1, int l1, char *file2, int s2, int l2, int
 //------------------------------------------------------------------
 void AFPCHAIN::Initial(void)
 {
+	errCode = PDB_OK;
+	errLine = 0;
+	errFile[0] = '\0';
 	pro1Len = pro2Len = afpNum = 0;
 	pro1 = pro2 = NULL;
 	iniTwistPdb = optTwistPdb = NULL;
@@ -1782,8 +1827,8 @@ void AFPCHAIN::WriteScript(PROT *p1, PROT *p2, int bk, int ifopt, char *pdbfile,
 			e1 = blockResList[i][0][blockResSize[i] - 1];
 			e2 = blockResList[i][1][blockResSize[i] - 1];
 		}
-		sprintf(region, "%s\nselect %s-%sA\ncolor [181,181,181]", region, p1->index[b1], p1->index[e1]);//gray
-		sprintf(region, "%s\nselect %s-%sB\ncolor [%d,%d,%d]\n", region, p2->index[b2], p2->index[e2], 
+		sprintf(region + strlen(region), "\nselect %s-%sA\ncolor [181,181,181]", p1->index[b1], p1->index[e1]);//gray
+		sprintf(region + strlen(region), "\nselect %s-%sB\ncolor [%d,%d,%d]\n", p2->index[b2], p2->index[e2], 
 				colorlist[3 * i], colorlist[3 * i + 1], colorlist[3 * i + 2]); //different color
 	}
 	ofstream script(scriptfile);
diff --git a/FATCATMain/AFPchain.h b/FATCATMain/AFPchain.h
index 01b68a6..3c893a2 100644
--- a/FATCATMain/AFPchain.h
+++ b/FATCATMain/AFPchain.h
@@ -161,9 +161,17 @@ class AFPCHAIN	{
 		void	SplitBlock(void);
 		void	GetAlign(void);
 	public:
-		AFPCHAIN(char *file1, char *file2, char *output);
-		AFPCHAIN(char *file1, char *file2, int sps, char *output);
-		AFPCHAIN(char *file1, int s1, int l1, char *file2, int s2, int l2, int sps, char *output);
+		PdbErr	errCode;
+		int	errLine;
+		char	errFile[1000];
+		void	Construct(char *file1, int s1, int l1, char *file2, int s2, int l2, int sps, char *output, PdbErr *err, int *lineNo, char *errFile);
+	public:
+		PdbErr	GetErr(void) const { return errCode; }
+		int	GetErrLine(void) const { return errLine; }
+		const char* GetErrFile(void) const { return errFile; }
+		AFPCHAIN(char *file1, char *file2, char *output, PdbErr *err = NULL, int *lineNo = NULL, char *errFile = NULL);
+		AFPCHAIN(char *file1, char *file2, int sps, char *output, PdbErr *err = NULL, int *lineNo = NULL, char *errFile = NULL);
+		AFPCHAIN(char *file1, int s1, int l1, char *file2, int s2, int l2, int sps, char *output, PdbErr *err = NULL, int *lineNo = NULL, char *errFile = NULL);
 		~AFPCHAIN();
 		void	ShowAfp(int ifcolor); //show AFP in ps format
 		void 	ChainAfp(void);	 //chain the AFPs
diff --git a/FATCATMain/FATCAT b/FATCATMain/FATCAT
index d496f73..02bfce8 100755
Binary files a/FATCATMain/FATCAT and b/FATCATMain/FATCAT differ
diff --git a/FATCATMain/FATCAT.C b/FATCATMain/FATCAT.C
index 7ce8fd5..1368591 100644
--- a/FATCATMain/FATCAT.C
+++ b/FATCATMain/FATCAT.C
@@ -81,7 +81,19 @@ int main(int argc, char *argv[])
 	char	pdb1f[100], pdb2f[100];
 	sprintf(pdb1f, "%s/%s", dir1, pdb1);
 	sprintf(pdb2f, "%s/%s", dir2, pdb2);
-	AFPCHAIN	*afpc = new AFPCHAIN(pdb1f, s1, l1, pdb2f, s2, l2, sparse, file0);
+	PdbErr	err = PDB_OK;
+	int	errLine = 0;
+	char	errFile[1000] = {0};
+	AFPCHAIN	*afpc = new AFPCHAIN(pdb1f, s1, l1, pdb2f, s2, l2, sparse, file0, &err, &errLine, errFile);
+	if (err != PDB_OK) {
+		if (errLine > 0) {
+			fprintf(stderr, "Error: %s at line %d in %s\n", pdbErrStr(err), errLine, errFile);
+		} else {
+			fprintf(stderr, "Error: %s in %s\n", pdbErrStr(err), errFile);
+		}
+		delete afpc;
+		exit(1);
+	}
 
 	if(showt)	afpc->ShowTimeSet();
 
diff --git a/FATCATMain/FATCAT2Tree b/FATCATMain/FATCAT2Tree
index 7d44421..299700e 100755
Binary files a/FATCATMain/FATCAT2Tree and b/FATCATMain/FATCAT2Tree differ
diff --git a/FATCATMain/FATCATDB b/FATCATMain/FATCATDB
index 4b93ece..c7ff711 100755
Binary files a/FATCATMain/FATCATDB and b/FATCATMain/FATCATDB differ
diff --git a/FATCATMain/FATPAT b/FATCATMain/FATPAT
index 698ed78..b775cf7 100755
Binary files a/FATCATMain/FATPAT and b/FATCATMain/FATPAT differ
diff --git a/FATCATMain/Makefile b/FATCATMain/Makefile
index 1b01ec2..697e529 100644
--- a/FATCATMain/Makefile
+++ b/FATCATMain/Makefile
@@ -1,5 +1,5 @@
 CC = g++
-CCOPT = -O2 -Wall
+CCOPT = -O2 -Wall -std=c++98
 LIBS = -lm
 INCDIRS = -I./
 CFLAGS = ${CCOPT} ${INCDIRS}
@@ -48,5 +48,5 @@ FATCAT2Tree :   $(OBJ_FATTREE)
 
 
 clean:
-	rm FATCAT FATCATDB PostFATCAT FATPAT
-	rm *.o
+	rm -f FATCAT FATCATDB PostFATCAT FATPAT FATCAT2Tree
+	rm -f *.o
diff --git a/FATCATMain/PostAlign.C b/FATCATMain/PostAlign.C
index 761e722..64d73f5 100644
--- a/FATCATMain/PostAlign.C
+++ b/FATCATMain/PostAlign.C
@@ -19,56 +19,112 @@ int colorlist[] = {255,0,0,  205,0,205,  255,255,0, 0,255,0,  0,0,255,  0,255,25
 //------------------------------------------------------------------
 //Constructors: assign two proteins and extract their AFPs
 //------------------------------------------------------------------
-POSTALIGN::POSTALIGN(char *file1, char *file2, char *name1, char *name2, char *alnfile)
+POSTALIGN::POSTALIGN(char *file1, char *file2, char *name1, char *name2, char *alnfile, PdbErr *err, int *lineNo, char *errFile)
 {
-	GetInput(file1, file2, name1, name2, alnfile, "fatcat");
-	//default: fatcat-alignment output
+	GetInput(file1, file2, name1, name2, alnfile, "fatcat", err, lineNo, errFile);
 }
 
-POSTALIGN::POSTALIGN(char *file1, char *file2, char *name1, char *name2, char *alnfile, char *format)
+POSTALIGN::POSTALIGN(char *file1, char *file2, char *name1, char *name2, char *alnfile, char *format, PdbErr *err, int *lineNo, char *errFile)
 {
-	GetInput(file1, file2, name1, name2, alnfile, format);
+	GetInput(file1, file2, name1, name2, alnfile, format, err, lineNo, errFile);
 }
 
 //------------------------------------------------------------------
-void POSTALIGN::GetInput(char *file1, char *file2, char *name1, char *name2, char *alnfile, char *format)
+PdbErr POSTALIGN::GetInput(char *file1, char *file2, char *name1, char *name2, char *alnfile, char *format, PdbErr *err, int *lineNo, char *errFile_out)
 {
 	Initial();
+	errCode = PDB_OK;
+	errLine = 0;
+	errFile[0] = '\0';
 
 	strcpy(pro1Name, name1);
 	strcpy(pro2Name, name2);
 
-	ReadPdb(file1, file2);
+	int eline = 0;
+	char efile[1000] = {0};
+	PdbErr e = ReadPdb(file1, file2, &eline, efile);
+	if(e != PDB_OK) {
+		errCode = e;
+		errLine = eline;
+		strcpy(errFile, efile);
+		if(err) *err = e;
+		if(lineNo) *lineNo = eline;
+		if(errFile_out) strcpy(errFile_out, efile);
+		if(err == NULL) {
+			if(eline > 0) fprintf(stderr, "Error: %s at line %d in %s\n", pdbErrStr(e), eline, efile);
+			else fprintf(stderr, "Error: %s in %s\n", pdbErrStr(e), efile);
+			exit(1);
+		}
+		return e;
+	}
 
 	Calloc();
 
-	if(!strcmp(format, "fatcat"))	ReadAln(alnfile, name1, name2);
-	else if(!strcmp(format, "fasta"))	ReadFastaAln(alnfile, name1, name2);
-	else if(!strcmp(format, "clustal"))	ReadClustalAln(alnfile, name1, name2);
-	else	{ printf("format %s is not supported currently\n", format); exit(1); }
+	if(!strcmp(format, "fatcat"))	e = ReadAln(alnfile, name1, name2, &eline, efile);
+	else if(!strcmp(format, "fasta"))	e = ReadFastaAln(alnfile, name1, name2, &eline, efile);
+	else if(!strcmp(format, "clustal"))	e = ReadClustalAln(alnfile, name1, name2, &eline, efile);
+	else	{
+		e = PDB_MALFORMED;
+		eline = 0;
+		strcpy(efile, alnfile);
+	}
+
+	if(e != PDB_OK) {
+		errCode = e;
+		errLine = eline;
+		strcpy(errFile, efile);
+		if(err) *err = e;
+		if(lineNo) *lineNo = eline;
+		if(errFile_out) strcpy(errFile_out, efile);
+		if(err == NULL) {
+			if(eline > 0) fprintf(stderr, "Error: %s at line %d in %s\n", pdbErrStr(e), eline, efile);
+			else fprintf(stderr, "Error: %s in %s\n", pdbErrStr(e), efile);
+			exit(1);
+		}
+		return e;
+	}
+
+	if(err) *err = PDB_OK;
+	return PDB_OK;
 }
 
 //------------------------------------------------------------------
-void POSTALIGN::ReadPdb(char *file1, char *file2)
+PdbErr POSTALIGN::ReadPdb(char *file1, char *file2, int *errLine, char *errFile)
 {
-	pro1 = new PROT(file1);
+	PdbErr e = PDB_OK;
+	int lno = 0;
+
+	pro1 = new PROT(file1, &e, &lno);
+	if(e != PDB_OK) {
+		if(errLine) *errLine = lno;
+		if(errFile) strcpy(errFile, file1);
+		return e;
+	}
 	pro1Len = pro1->GetLength();
 	proseq1 = new char[pro1Len + 1];
 	pro1->GetSAA(proseq1);
 
-	pro2 = new PROT(file2);
+	pro2 = new PROT(file2, &e, &lno);
+	if(e != PDB_OK) {
+		if(errLine) *errLine = lno;
+		if(errFile) strcpy(errFile, file2);
+		return e;
+	}
 	pro2Len = pro2->GetLength();
 	proseq2 = new char[pro2Len + 1];
 	pro2->GetSAA(proseq2);
 
 	if(pro1Len <= 0 || pro2Len <= 0)	{
-		printf("Wrong pdb read %d %d\n", pro1Len, pro2Len);
-		exit(1);
+		char *bad = (pro1Len <= 0) ? file1 : file2;
+		if(errLine) *errLine = 0;
+		if(errFile) strcpy(errFile, bad);
+		return PDB_TRUNCATED;
 	}
 
 	if(pro1Len < pro2Len)	minLen = pro1Len;
 	else	minLen = pro2Len;
 
+	return PDB_OK;
 }
 
 //------------------------------------------------------------------
@@ -76,6 +132,9 @@ void POSTALIGN::ReadPdb(char *file1, char *file2)
 //------------------------------------------------------------------
 void POSTALIGN::Initial(void)
 {
+	errCode = PDB_OK;
+	errLine = 0;
+	errFile[0] = '\0';
 	pro1 = NULL;
 	pro2 = NULL;
 	proseq1 = proseq2 = NULL;
@@ -197,7 +256,7 @@ void POSTALIGN::GetHotSpot(int num, char **hotspot)
 }
 
 //------------------------------------------------------------------
-void POSTALIGN::ReadAln(char *alnfile, char *name1, char *name2)
+PdbErr POSTALIGN::ReadAln(char *alnfile, char *name1, char *name2, int *errLine, char *errFile)
 {
 	UniName(name1);
 	UniName(name2);
@@ -205,19 +264,21 @@ void POSTALIGN::ReadAln(char *alnfile, char *name1, char *name2)
 	char	str[1000];
 	ifstream in(alnfile);
 	if(!in)	{
-		printf("open file %s error\n", alnfile);
-		exit(1);
+		if(errLine) *errLine = 0;
+		if(errFile) strcpy(errFile, alnfile);
+		return PDB_OPEN_ERR;
 	}
 	int	len1, len2, reverse;
 	int	lab = 0;
+	int	lineNo = 0;
 	char	code1[200], code2[200], str2[201], str3[201], blockseq[20000];
 	blockNum = reverse = 0;
 	alnseq1[0] = alnseq2[0] = blockseq[0] = 0;
-	while(!in.eof())	{
-		str[0] = 0;
-		in.getline(str, 200);
+	size_t maxSeqLen = (size_t)(pro1Len + pro2Len);
+	while(in.getline(str, 200))	{
+		lineNo++;
 		if(!strncmp(str, "Align", 5))	{
-			sscanf(str, "%*s%s%d%*s%s%d", code1, &len1, code2, &len2);
+			sscanf(str, "%*s%199s%d%*s%199s%d", code1, &len1, code2, &len2);
                         UniName(code1);
                         UniName(code2);
 			if(!strcmp(code1, name1) && !strcmp(code2, name2))	{ 
@@ -231,8 +292,19 @@ void POSTALIGN::ReadAln(char *alnfile, char *name1, char *name2)
 			else	lab = 0;
 		}
 		else if(!strncmp(str, "Chain 1", 7) && lab)	{
+			str2[0] = str3[0] = 0;
 			in.getline(str2, 200);
+			lineNo++;
 			in.getline(str3, 200);
+			lineNo++;
+			if(strlen(blockseq) + strlen(str2 + 14) >= sizeof(blockseq) ||
+			   strlen(alnseq1) + strlen(str + 14) > maxSeqLen ||
+			   strlen(alnseq2) + strlen(str3 + 14) > maxSeqLen) {
+				in.close();
+				if(errLine) *errLine = lineNo;
+				if(errFile) strcpy(errFile, alnfile);
+				return PDB_TOO_LONG;
+			}
 			strcat(blockseq, str2 + 14);
 			if(reverse == 0)	{
 				strcat(alnseq1, str + 14);
@@ -250,11 +322,13 @@ void POSTALIGN::ReadAln(char *alnfile, char *name1, char *name2)
 	in.close();
 
 	if(lab == 0)	{ 
-		printf("No alignment is found between the given proteins %s %s, in %s\n", name1, name2, alnfile); 
-		exit(1); 
+		if(errLine) *errLine = 0;
+		if(errFile) strcpy(errFile, alnfile);
+		return PDB_MALFORMED;
 	}
 
 	GetAlignPos(blockseq);
+	return PDB_OK;
 }
 
 void POSTALIGN::GetAlignPos(char *blockseq)
@@ -507,8 +581,8 @@ void POSTALIGN::WriteScript(PROT *p1, PROT *p2, int bk, int ifopt, char *pdbfile
 			e1 = optAln[i][0][optLen[i] - 1];
 			e2 = optAln[i][1][optLen[i] - 1];
 		}
-		sprintf(region, "%s\nselect %s-%sA\ncolor [181,181,181]", region, p1->index[b1], p1->index[e1]);//gray
-		sprintf(region, "%s\nselect %s-%sB\ncolor [%d,%d,%d]\n", region, p2->index[b2], p2->index[e2], 
+		sprintf(region + strlen(region), "\nselect %s-%sA\ncolor [181,181,181]", p1->index[b1], p1->index[e1]);//gray
+		sprintf(region + strlen(region), "\nselect %s-%sB\ncolor [%d,%d,%d]\n", p2->index[b2], p2->index[e2], 
 				colorlist[3 * i], colorlist[3 * i + 1], colorlist[3 * i + 2]); //different color
 	}
 	ofstream script(scriptfile);
@@ -664,18 +738,25 @@ void POSTALIGN::WriteMatrix(char *outfile)
 	out.close();
 }
 
-void POSTALIGN::ReadFastaAln(char *file, char *name1, char *name2)
+PdbErr POSTALIGN::ReadFastaAln(char *file, char *name1, char *name2, int *errLine, char *errFile)
 {
 	//read alignment file in fasta format
 	ifstream in(file);
-	if(!in)	{ printf("open %s error\n", file); exit(1); }
+	if(!in)	{
+		if(errLine) *errLine = 0;
+		if(errFile) strcpy(errFile, file);
+		return PDB_OPEN_ERR;
+	}
 	int	lab = 0;
+	int	lineNo = 0;
 	alnseq1[0] = alnseq2[0] = 0;
 	char	str[1001], tmp[1000];
-	while(!in.eof())	{
-		in.getline(str, 1000);
+	size_t maxSeqLen = (size_t)(pro1Len + pro2Len);
+	while(in.getline(str, 1000))	{
+		lineNo++;
 		if(str[0] == '>')	{ 
-			sscanf(str + 1, "%s", tmp);
+			tmp[0] = '\0';
+			sscanf(str + 1, "%999s", tmp);
 			if(!strcmp(tmp, name1))	{	
 				lab = 1;
 			}
@@ -686,60 +767,101 @@ void POSTALIGN::ReadFastaAln(char *file, char *name1, char *name2)
 		}
 		else if(strlen(str) >= 1)	{
 			if(lab == 1)	{
+				if(strlen(alnseq1) + strlen(str) > maxSeqLen) {
+					in.close();
+					if(errLine) *errLine = lineNo;
+					if(errFile) strcpy(errFile, file);
+					return PDB_TOO_LONG;
+				}
 				strcat(alnseq1, str);
 			}
 			else if(lab == 2)	{
+				if(strlen(alnseq2) + strlen(str) > maxSeqLen) {
+					in.close();
+					if(errLine) *errLine = lineNo;
+					if(errFile) strcpy(errFile, file);
+					return PDB_TOO_LONG;
+				}
 				strcat(alnseq2, str);
 			}
 		}
 	}
 	in.close();
 
-	if(alnseq1[0] == 0)	{ printf("can not find %s sequence in the alignment file or the format is not correct\n", name1); exit(1); }
-	if(alnseq2[0] == 0)	{ printf("can not find %s sequence in the alignment file or the format is not correct\n", name2); exit(1); }
+	if(alnseq1[0] == 0 || alnseq2[0] == 0)	{
+		if(errLine) *errLine = 0;
+		if(errFile) strcpy(errFile, file);
+		return PDB_MALFORMED;
+	}
 
 	int	len = strlen(alnseq1);
 	char	*blockseq = new char[len + 1];
 	int	i;
 	for(i = 0; i < len; i ++)	blockseq[i] = '1'; //in this case, no block information 
+	blockseq[len] = '\0';
 
 	GetAlignPos(blockseq);
 
 	delete[] blockseq;
+	return PDB_OK;
 }
 
-void POSTALIGN::ReadClustalAln(char *file, char *name1, char *name2)
+PdbErr POSTALIGN::ReadClustalAln(char *file, char *name1, char *name2, int *errLine, char *errFile)
 {
 	//read alignment file in clustal format
 	ifstream in(file);
-	if(!in)	{ printf("open %s error\n", file); exit(1); }
+	if(!in)	{
+		if(errLine) *errLine = 0;
+		if(errFile) strcpy(errFile, file);
+		return PDB_OPEN_ERR;
+	}
 	alnseq1[0] = alnseq2[0] = 0;
 	char	str[1001], tmp[1000], tmp2[1000];
-	while(!in.eof())	{
-		in.getline(str, 1000);
+	int	lineNo = 0;
+	size_t maxSeqLen = (size_t)(pro1Len + pro2Len);
+	while(in.getline(str, 1000))	{
+		lineNo++;
 		if(strlen(str) > 3)	{
-			sscanf(str, "%s", tmp);
+			tmp[0] = tmp2[0] = '\0';
+			sscanf(str, "%999s", tmp);
 			if(!strcmp(tmp, name1))	{	
-				sscanf(str, "%*s%s", tmp2);
+				sscanf(str, "%*s%999s", tmp2);
+				if(strlen(alnseq1) + strlen(tmp2) > maxSeqLen) {
+					in.close();
+					if(errLine) *errLine = lineNo;
+					if(errFile) strcpy(errFile, file);
+					return PDB_TOO_LONG;
+				}
 				strcat(alnseq1, tmp2);
 			}
 			else if(!strcmp(tmp, name2))	{
-				sscanf(str, "%*s%s", tmp2);
+				sscanf(str, "%*s%999s", tmp2);
+				if(strlen(alnseq2) + strlen(tmp2) > maxSeqLen) {
+					in.close();
+					if(errLine) *errLine = lineNo;
+					if(errFile) strcpy(errFile, file);
+					return PDB_TOO_LONG;
+				}
 				strcat(alnseq2, tmp2);
 			}
 		}
 	}
 	in.close();
 
-	if(alnseq1[0] == 0)	{ printf("can not find %s sequence in the alignment file or the format is not correct\n", name1); exit(1); }
-	if(alnseq2[0] == 0)	{ printf("can not find %s sequence in the alignment file or the format is not correct\n", name2); exit(1); }
+	if(alnseq1[0] == 0 || alnseq2[0] == 0)	{
+		if(errLine) *errLine = 0;
+		if(errFile) strcpy(errFile, file);
+		return PDB_MALFORMED;
+	}
 
 	int	len = strlen(alnseq1);
 	char	*blockseq = new char[len + 1];
 	int	i;
 	for(i = 0; i < len; i ++)	blockseq[i] = '1'; //in this case, no block information 
+	blockseq[len] = '\0';
 
 	GetAlignPos(blockseq);
 
 	delete[] blockseq;
+	return PDB_OK;
 }
diff --git a/FATCATMain/PostAlign.h b/FATCATMain/PostAlign.h
index a7104ff..21c723d 100644
--- a/FATCATMain/PostAlign.h
+++ b/FATCATMain/PostAlign.h
@@ -54,6 +54,10 @@ class POSTALIGN	{
 		double	*translateList;
 		double	*rmsdList;
 
+		PdbErr	errCode;
+		int	errLine;
+		char	errFile[1000];
+
 		void	Initial(void);	//initial the class
 		void	Calloc(void);	//calloc the variants
 		void	WriteScript(PROT *p1, PROT *p2, int bk, int ifopt, char *pdbfile, char *scriptfile);
@@ -62,14 +66,17 @@ class POSTALIGN	{
 		void	WritePdb(char *info, char *pdbfile);
 		void	WritePdb(char *info, char *pdbfile, PROT *p1, PROT *p2);
 	public:
-		POSTALIGN(char *file1, char *file2, char *name1, char *name2, char *alnfile);
-		POSTALIGN(char *file1, char *file2, char *name1, char *name2, char *alnfile, char *format);
+		PdbErr	GetErr(void) const { return errCode; }
+		int	GetErrLine(void) const { return errLine; }
+		const char* GetErrFile(void) const { return errFile; }
+		POSTALIGN(char *file1, char *file2, char *name1, char *name2, char *alnfile, PdbErr *err = NULL, int *lineNo = NULL, char *errFile = NULL);
+		POSTALIGN(char *file1, char *file2, char *name1, char *name2, char *alnfile, char *format, PdbErr *err = NULL, int *lineNo = NULL, char *errFile = NULL);
 		~POSTALIGN();
-		void	GetInput(char *file1, char *file2, char *name1, char *name2, char *alnfile, char *format);
-		void	ReadPdb(char *file1, char *file2);
-		void	ReadAln(char *alnfile, char *name1, char *name2);
-		void	ReadFastaAln(char *alnfile, char *name1, char *name2);
-		void	ReadClustalAln(char *alnfile, char *name1, char *name2);
+		PdbErr	GetInput(char *file1, char *file2, char *name1, char *name2, char *alnfile, char *format, PdbErr *err = NULL, int *lineNo = NULL, char *errFile = NULL);
+		PdbErr	ReadPdb(char *file1, char *file2, int *errLine = NULL, char *errFile = NULL);
+		PdbErr	ReadAln(char *alnfile, char *name1, char *name2, int *errLine = NULL, char *errFile = NULL);
+		PdbErr	ReadFastaAln(char *alnfile, char *name1, char *name2, int *errLine = NULL, char *errFile = NULL);
+		PdbErr	ReadClustalAln(char *alnfile, char *name1, char *name2, int *errLine = NULL, char *errFile = NULL);
 		void	GetAlignPos(char *blockseq);
 		void	UniName(char *name);
 		char*	Aln2Seq(char *alnseq);
diff --git a/FATCATMain/PostFATCAT b/FATCATMain/PostFATCAT
index b1af27a..6e38673 100755
Binary files a/FATCATMain/PostFATCAT and b/FATCATMain/PostFATCAT differ
diff --git a/FATCATMain/PostFATCAT.C b/FATCATMain/PostFATCAT.C
index bb36c05..83c871a 100644
--- a/FATCATMain/PostFATCAT.C
+++ b/FATCATMain/PostFATCAT.C
@@ -50,9 +50,21 @@ int main(int argc, char *argv[])
 		printf("no function is defined\n");
 		exit(1);
 	}
-	POSTALIGN	*palign;
-	if(ref == 1) 	palign = new POSTALIGN(pdb1file, pdb2file, name1, name2, alignfile);
-	else 		palign = new POSTALIGN(pdb2file, pdb1file, name2, name1, alignfile);
+	POSTALIGN	*palign = NULL;
+	PdbErr		err = PDB_OK;
+	int		errLine = 0;
+	char		errFile[1000] = {0};
+	if(ref == 1) 	palign = new POSTALIGN(pdb1file, pdb2file, name1, name2, alignfile, &err, &errLine, errFile);
+	else 		palign = new POSTALIGN(pdb2file, pdb1file, name2, name1, alignfile, &err, &errLine, errFile);
+	if(err != PDB_OK) {
+		if(errLine > 0) {
+			fprintf(stderr, "Error: %s at line %d in %s\n", pdbErrStr(err), errLine, errFile);
+		} else {
+			fprintf(stderr, "Error: %s in %s\n", pdbErrStr(err), errFile);
+		}
+		if(palign) delete palign;
+		exit(1);
+	}
 	if(cpdb[0] != 0 && rasmol[0] != 0)	palign -> WriteTwistPdb(cpdb, chain1[0], chain2[0], rasmol);  
 	else if(cpdb[0] != 0)	palign -> WriteTwistPdb(cpdb, chain1[0], chain2[0]);
 	if(outfile[0] != 0)	{
diff --git a/FATCATMain/Prot.C b/FATCATMain/Prot.C
index a7b56ee..14872cf 100644
--- a/FATCATMain/Prot.C
+++ b/FATCATMain/Prot.C
@@ -4,28 +4,77 @@
 //--------------------------------------------------------------
 #include <cstring>
 #include <cstdlib>
+#include <cstdio>
 #include "Prot.h"
 #include "Amino.h"
 using namespace AMINO;
+
+const char* pdbErrStr(PdbErr err)
+{
+	switch (err) {
+		case PDB_OK:
+			return "ok";
+		case PDB_MALFORMED:
+			return "malformed PDB format";
+		case PDB_TRUNCATED:
+			return "truncated PDB file";
+		case PDB_TOO_LONG:
+			return "protein length exceeds MAXLEN";
+		case PDB_ALLOC:
+			return "memory allocation failure";
+		case PDB_OPEN_ERR:
+			return "failed to open file";
+		default:
+			return "unknown error";
+	}
+}
+
 //--------------------------------------------------------------
-PROT::PROT(char *file)
+PROT::PROT(char *file, PdbErr *err, int *lineNo)
 {
 	Initial();
-	AssignPdb(file, 0, 100000);
-	SAA();
+	int lno = 0;
+	PdbErr e = AssignPdb(file, 0, 100000, &lno);
+	errCode = e;
+	errLine = lno;
+	if(err) *err = e;
+	if(lineNo) *lineNo = lno;
+	if(e == PDB_OK) {
+		SAA();
+	} else if(err == NULL) {
+		if(lno > 0)
+			fprintf(stderr, "Error: %s at line %d in %s\n", pdbErrStr(e), lno, file);
+		else
+			fprintf(stderr, "Error: %s in %s\n", pdbErrStr(e), file);
+		exit(1);
+	}
 }
 
 //--------------------------------------------------------------
-PROT::PROT(char *file, int begres, int reslen)
+PROT::PROT(char *file, int begres, int reslen, PdbErr *err, int *lineNo)
 {
 	Initial();
-	AssignPdb(file, begres, reslen);
-	SAA();
+	int lno = 0;
+	PdbErr e = AssignPdb(file, begres, reslen, &lno);
+	errCode = e;
+	errLine = lno;
+	if(err) *err = e;
+	if(lineNo) *lineNo = lno;
+	if(e == PDB_OK) {
+		SAA();
+	} else if(err == NULL) {
+		if(lno > 0)
+			fprintf(stderr, "Error: %s at line %d in %s\n", pdbErrStr(e), lno, file);
+		else
+			fprintf(stderr, "Error: %s in %s\n", pdbErrStr(e), file);
+		exit(1);
+	}
 }
 
 //--------------------------------------------------------------
 void PROT::SAA(void)
 {
+	if(length <= 0 || res == NULL) return;
 	saa = NewArray <char> (length + 1);
 	int	i;
 	for(i = 0; i < length; i ++)	{
@@ -37,7 +86,8 @@ void PROT::SAA(void)
 //--------------------------------------------------------------
 void PROT::GetSAA(char *saa_o)
 {
-	strcpy(saa_o, saa); 
+	if(saa != NULL) strcpy(saa_o, saa);
+	else saa_o[0] = '\0';
 }
 
 //--------------------------------------------------------------
@@ -53,8 +103,10 @@ void PROT::GetIndex(int *index_o)
 //--------------------------------------------------------------
 int  PROT::GetIndex(int index_i)
 {
-	int	i;
-	sscanf(index[index_i], "%d", &i);
+	int	i = 0;
+	if(index_i >= 0 && index_i < length && index != NULL) {
+		sscanf(index[index_i], "%d", &i);
+	}
 	return i;
 }
 
@@ -75,25 +127,36 @@ PROT::PROT(void)
 	Initial();
 }
 
+//--------------------------------------------------------------
+void PROT::Free(void)
+{
+	if(res != NULL)		{ DelMatrix <char> (res, length0); res = NULL; }
+	if(chain != NULL)	{ DelArray <char> (chain); chain = NULL; }
+	if(index != NULL)	{ DelMatrix <char> (index, length0); index = NULL; }
+	if(caCod != NULL)	{ DelArray <double> (caCod); caCod = NULL; }
+	if(asa != NULL)		{ DelArray <double> (asa); asa = NULL; }
+	if(res2Atm != NULL)	{ DelArray <int> (res2Atm); res2Atm = NULL; }
+	if(atmCod != NULL)	{ DelArray <double> (atmCod); atmCod = NULL; }
+	if(atmName != NULL)	{ DelMatrix <char> (atmName, atm0); atmName = NULL; }
+	if(atm2Res != NULL)	{ DelArray <int> (atm2Res); atm2Res = NULL; }
+	if(saa != NULL)		{ DelArray <char> (saa); saa = NULL; }
+	length = 0;
+	length0 = 0;
+	totalAtm = 0;
+	atm0 = 0;
+}
+
 //--------------------------------------------------------------
 PROT::~PROT(void)
 {
-	if(res != NULL)		DelMatrix <char> (res, length0);
-	if(chain != NULL)	DelArray <char> (chain);
-	if(index != NULL)	DelMatrix <char> (index, length0);
-	if(caCod != NULL)	DelArray <double> (caCod);	
-	if(asa != NULL)		DelArray <double> (asa);	
-	if(res2Atm != NULL)	DelArray <int> (res2Atm);
-	if(atmCod != NULL)	DelArray <double> (atmCod);
-	if(atmName != NULL)	DelMatrix <char> (atmName, atm0);
-	if(atm2Res != NULL)	DelArray <int> (atm2Res);
-	if(saa != NULL)		DelArray <char> (saa);
+	Free();
 }
 
 //--------------------------------------------------------------
 void PROT::Initial(void)
 {
 	length = 0;
+	length0 = 0;
 	res = NULL;
 	chain = NULL;
 	index = NULL;
@@ -102,10 +165,14 @@ void PROT::Initial(void)
 	res2Atm = NULL;
 
 	totalAtm = 0;
+	atm0 = 0;
 	atmCod = NULL;
 	atmName = NULL;
 	atm2Res = NULL;
 	saa = NULL;
+
+	errCode = PDB_OK;
+	errLine = 0;
 }
 
 //--------------------------------------------------------------
@@ -133,108 +200,208 @@ void PROT::CallocAtm(int atm)
 
 //read processed-pdb files with only Ca coordination
 //--------------------------------------------------------------
-void PROT::AssignPdbCa(char *file)
+PdbErr PROT::AssignPdbCa(char *file, int *errLine)
 {
+	if (errLine) *errLine = 0;
+	if (!file) return PDB_OPEN_ERR;
 	int	len1, len2;
 	len1 = len2 = 0;
 	char	str[50000];
 	ifstream pdb(file);
-	if(!pdb) {  printf("open pdb file error in AssignPdbCa\n"); exit(1); }
+	if(!pdb) {
+		if (errLine) *errLine = 0;
+		return PDB_OPEN_ERR;
+	}
+	int lineNo = 0;
 	while(!pdb.eof())	{
-		pdb.getline(str, 5000);
+		str[0] = '\0';
+		pdb.getline(str, sizeof(str));
+		if (pdb.gcount() == 0 && pdb.eof()) break;
+		lineNo++;
 		if(!strncmp(str, "#Length", 7))	{
 			sscanf(str, "%*s%d", &len1);
+			if (len1 > MAXLEN) {
+				pdb.close();
+				if (errLine) *errLine = lineNo;
+				return PDB_TOO_LONG;
+			}
 			Calloc(len1);
 		}
 		if(str[0] == '#' || str[0] == '\0')	continue;
-		if(caCod == NULL) { printf("caCod is not calloced in AssignPdbCa\n"); exit(1); }	
-		if(len2 >= length) { printf("wrong in residue number in AssignPdbCa\n"); exit(1); }	
-		sscanf(str, "%s", index[len2]);
-		sscanf(str + 8, "%s%lf%lf%lf\n", res[len2], 
-			&caCod[3 * len2], &caCod[3 * len2 + 1], &caCod[3 * len2 + 2]);
+		if(caCod == NULL) {
+			pdb.close();
+			if (errLine) *errLine = lineNo;
+			return PDB_MALFORMED;
+		}	
+		if(len2 >= length) {
+			pdb.close();
+			if (errLine) *errLine = lineNo;
+			return PDB_MALFORMED;
+		}	
+		if (sscanf(str, "%4s", index[len2]) != 1 ||
+		    sscanf(str + 8, "%4s%lf%lf%lf", res[len2], 
+			&caCod[3 * len2], &caCod[3 * len2 + 1], &caCod[3 * len2 + 2]) != 4) {
+			pdb.close();
+			if (errLine) *errLine = lineNo;
+			return PDB_MALFORMED;
+		}
 		len2 ++;
 	}
 	pdb.close();
+	if (len2 == 0) {
+		if (errLine) *errLine = 0;
+		return PDB_TRUNCATED;
+	}
+	return PDB_OK;
 }
 
 //read original-pdb files with all atoms 
 //--------------------------------------------------------------
-void PROT::AssignPdb(char *file, int begres, int reslen)
+PdbErr PROT::AssignPdb(char *file, int begres, int reslen, int *errLine)
 {
-//printf("file %s, begres %d, reslen %d\n", file, begres, reslen);
+	if (errLine) *errLine = 0;
+	if (!file) return PDB_OPEN_ERR;
 
 	int	i, len, atm, ca, k;
 	char	str[50000], oldline[50000];
+	oldline[0] = '\0';
+
 	ifstream pdb(file);
 	if(!pdb) {
-		printf("open pdb file %s error\n", file);
-		exit(1);
+		if (errLine) *errLine = 0;
+		return PDB_OPEN_ERR;
 	}
 
 	//extract length of residues and atoms first
 	len = atm = 0;
+	int lineNo = 0;
 	while(!pdb.eof())	{
 		str[0] = '\0';
-		pdb.getline(str, 5000);
+		pdb.getline(str, sizeof(str));
+		if (pdb.gcount() == 0 && pdb.eof()) break;
+		lineNo++;
 		if(!strncmp(str, "ENDMDL", 6))	break;
 		if(!strncmp(str, "ATOM", 4))	{
-			if(strncmp(str + 22, oldline + 22, 5) || len == 0)	len ++;	
-			//bug reported by Martin for case d1tlca_
+			int slen = strlen(str);
+			if (slen < 54) {
+				pdb.close();
+				if (errLine) *errLine = lineNo;
+				return PDB_TRUNCATED;
+			}
+			char testAtm[10] = {0}, testRes[10] = {0}, testIdx[10] = {0};
+			double x, y, z;
+			if (sscanf(str + 12, "%5s", testAtm) != 1 ||
+			    sscanf(str + 17, "%4s", testRes) != 1 ||
+			    sscanf(str + 22, "%4s", testIdx) != 1 ||
+			    sscanf(str + 30, "%lf%lf%lf", &x, &y, &z) != 3) {
+				pdb.close();
+				if (errLine) *errLine = lineNo;
+				return PDB_MALFORMED;
+			}
+			if(len == 0 || strncmp(str + 22, oldline + 22, 5)) {
+				len ++;
+				if(len > MAXLEN) {
+					pdb.close();
+					if (errLine) *errLine = lineNo;
+					return PDB_TOO_LONG;
+				}
+			}
 			atm ++;
-		}	
-		strcpy(oldline, str);
+			strncpy(oldline, str, sizeof(oldline) - 1);
+			oldline[sizeof(oldline) - 1] = '\0';
+		}
 	}
 	pdb.close();
 
-	//calloc the varibles
+	if(len == 0 || atm == 0) {
+		if (errLine) *errLine = 0;
+		return PDB_TRUNCATED;
+	}
+
+	//calloc the variables
 	Calloc(len);
 	CallocAtm(atm);
 
-	//assign the varibles
+	if(res == NULL || chain == NULL || index == NULL || caCod == NULL || asa == NULL ||
+	   res2Atm == NULL || atmCod == NULL || atmName == NULL || atm2Res == NULL) {
+		Free();
+		if (errLine) *errLine = 0;
+		return PDB_ALLOC;
+	}
+
+	//assign the variables
 	int	*caread = NewArray <int> (len);
+	int	*resLine = NewArray <int> (len);
+	if (caread == NULL || resLine == NULL) {
+		if (caread) DelArray <int> (caread);
+		if (resLine) DelArray <int> (resLine);
+		Free();
+		if (errLine) *errLine = 0;
+		return PDB_ALLOC;
+	}
+
 	len = atm = ca = 0;
 	int	add = -1;
 	int	len_bf, atm_bf;
 	len_bf = atm_bf = 0;
+	int	lineNo2 = 0;
+	oldline[0] = '\0';
 	ifstream pdb2(file);
+	if(!pdb2) {
+		DelArray <int> (caread);
+		DelArray <int> (resLine);
+		Free();
+		if (errLine) *errLine = 0;
+		return PDB_OPEN_ERR;
+	}
 	while(!pdb2.eof())	{
 		str[0] = '\0';
-		pdb2.getline(str, 5000);
+		pdb2.getline(str, sizeof(str));
+		if (pdb2.gcount() == 0 && pdb2.eof()) break;
+		lineNo2++;
 		if(!strncmp(str, "ENDMDL", 6))	break;
 		if(!strncmp(str, "ATOM", 4))	{
 			if(add == -1 || strncmp(str + 22, oldline + 22, 5))	{
 				add ++;
 				if(add < begres)	{
-					strcpy(oldline, str);
+					strncpy(oldline, str, sizeof(oldline) - 1);
+					oldline[sizeof(oldline) - 1] = '\0';
 					continue;	
 				}
 				if(add >= reslen + begres)	break; 
 					//note: only part structure when begres and reslen are used	
 				if(len >= length0)	{
-					printf("length0 %d\nthis line %s\n", length0, str);
-					printf("wrong residue read\n");
-					exit(1);
+					pdb2.close();
+					DelArray <int> (caread);
+					DelArray <int> (resLine);
+					Free();
+					if (errLine) *errLine = lineNo2;
+					return PDB_MALFORMED;
 				}
 				if(len >= 1 && caread[len - 1] == 0)	{
-					//printf("Warning: pdb %s, the %s residue has no CA atom, removed!\n", file, index[len - 1]);
 					len = len_bf;
 					atm = atm_bf;
 				} //the residues without CA atom is not considered, Y.Y, Dec 8, 03
-				sscanf(str + 17, "%s", res[len]);
-				sscanf(str + 22, "%s", index[len]);
+				sscanf(str + 17, "%4s", res[len]);
+				sscanf(str + 22, "%4s", index[len]);
 				res2Atm[len] = atm;
 				chain[len] = str[21];
 				caread[len] = 0;
+				resLine[len] = lineNo2;
 				len_bf = len;
 				atm_bf = atm;
 				len ++;	
 			} //a new residue
 			if(add < begres)	continue;	
 			if(atm >= atm0)	{
-				printf("wrong atm read\n");
-				exit(1);
+				pdb2.close();
+				DelArray <int> (caread);
+				DelArray <int> (resLine);
+				Free();
+				if (errLine) *errLine = lineNo2;
+				return PDB_MALFORMED;
 			}
-			sscanf(str + 12, "%s", atmName[atm]);
+			sscanf(str + 12, "%5s", atmName[atm]);
 			sscanf(str + 30, "%lf%lf%lf", &atmCod[3*atm], &atmCod[3*atm+1], &atmCod[3*atm+2]);
 			if(!strcmp(atmName[atm], "CA"))	{
 				if(caread[len - 1] == 0)	{
@@ -246,10 +413,10 @@ void PROT::AssignPdb(char *file, int begres, int reslen)
 			atm2Res[atm] = len - 1;
 			atm ++;
 		}	
-		strcpy(oldline, str);
+		strncpy(oldline, str, sizeof(oldline) - 1);
+		oldline[sizeof(oldline) - 1] = '\0';
 	}
 	if(len >= 1 && caread[len - 1] == 0)	{
-		//printf("Warning: pdb %s, the %s residue has no CA atom, removed!\n", file, index[len - 1]);
 		len = len_bf;
 		atm = atm_bf;
 	}
@@ -260,29 +427,48 @@ void PROT::AssignPdb(char *file, int begres, int reslen)
 	length = len;
 	totalAtm = atm;
 
+	if(len == 0) {
+		DelArray <int> (caread);
+		DelArray <int> (resLine);
+		Free();
+		if (errLine) *errLine = 0;
+		return PDB_TRUNCATED;
+	}
 
-	if(ca != len)	{
-		for(i = 0; i < len; i ++) {
-			if(caread[i] == 0)	{
-				printf("Warning: pdb %s, the %s residue has no CA atom!\n", file, index[i]); exit(1);
-			}
-			if(caread[i] > 1)	{
-				printf("Warning: pdb %s, the %s residue has two CA atom!\n", file, index[i]); exit(1);
-			}
+	for(i = 0; i < len; i ++) {
+		if(caread[i] == 0 || caread[i] > 1) {
+			int el = resLine[i];
+			DelArray <int> (caread);
+			DelArray <int> (resLine);
+			Free();
+			if (errLine) *errLine = el;
+			return PDB_MALFORMED;
 		}
-		//sprintf(str, "read pdb %s error: %d residues have no CA atoms, residue %d, ca %d!\n", file, len - ca, len, ca);
-		//DelArray <int> (caread);
-		//Y.Y, Dec,8,2003, annoyed by so many not-perfect pdb files
+	}
+	if(ca != len) {
+		DelArray <int> (caread);
+		DelArray <int> (resLine);
+		Free();
+		if (errLine) *errLine = 0;
+		return PDB_MALFORMED;
 	}
 	DelArray <int> (caread);
+	DelArray <int> (resLine);
+	return PDB_OK;
 }
 
 //--------------------------------------------------------------
-void PROT::AssignPdb(PROT *pro)
+PdbErr PROT::AssignPdb(PROT *pro)
 {
+	if (!pro || pro->length <= 0) return PDB_MALFORMED;
 	//calloc the varibles
 	Calloc(pro->length);
 	CallocAtm(pro->totalAtm);
+	if (res == NULL || chain == NULL || index == NULL || caCod == NULL || asa == NULL ||
+	    res2Atm == NULL || atmCod == NULL || atmName == NULL || atm2Res == NULL) {
+		Free();
+		return PDB_ALLOC;
+	}
 
 	//assign coordinations and residues from input pdb
 	int	i, k;
@@ -304,6 +490,7 @@ void PROT::AssignPdb(PROT *pro)
 		strcpy(atmName[i], pro->atmName[i]);
 		atm2Res[i] = pro->atm2Res[i];
 	}
+	return PDB_OK;
 }
 
 //output the pdb to the stream
diff --git a/FATCATMain/Prot.h b/FATCATMain/Prot.h
index 9ca74b1..aa0c477 100644
--- a/FATCATMain/Prot.h
+++ b/FATCATMain/Prot.h
@@ -16,6 +16,18 @@ class MULTAFPCHAIN;
 class POSTALIGN;
 class LSVECTOR;
 
+enum PdbErr {
+	PDB_OK = 0,
+	PDB_MALFORMED,
+	PDB_TRUNCATED,
+	PDB_TOO_LONG,
+	PDB_ALLOC,
+	PDB_OPEN_ERR
+};
+
+const char* pdbErrStr(PdbErr err);
+inline const char* enum_to_string(PdbErr err) { return pdbErrStr(err); }
+
 class PROT	{
 	protected:
 		int	length;	  //length of the protein
@@ -35,18 +47,24 @@ class PROT	{
 		int	length0;
 		int	atm0;
 
+		PdbErr	errCode;
+		int	errLine;
+
 		void	Initial(void);
 		void	Calloc(int len);
 		void	CallocAtm(int atm);
-		void	AssignPdbCa(char *file);  //read processed pdb file with only CA coords.
+		void	Free(void);
+		PdbErr	AssignPdbCa(char *file, int *errLine = NULL);  //read processed pdb file with only CA coords.
 	public:
-		PROT(char *filename);
-		PROT(char *filename, int begres, int reslen);
+		PROT(char *filename, PdbErr *err = NULL, int *lineNo = NULL);
+		PROT(char *filename, int begres, int reslen, PdbErr *err = NULL, int *lineNo = NULL);
 		PROT(void);
 		~PROT(void);
+		PdbErr	GetErr(void) const { return errCode; }
+		int	GetErrLine(void) const { return errLine; }
 		void	SAA(void);
-		void	AssignPdb(char *file, int begres, int reslen); //read original pdb file
-		void	AssignPdb(PROT *pro); //read original pdb file
+		PdbErr	AssignPdb(char *file, int begres, int reslen, int *errLine = NULL); //read original pdb file
+		PdbErr	AssignPdb(PROT *pro); //read original pdb file
 		double* Cod4Res(int n, int *list); //return the coordinations for a given residue list
 		void	PrintPdb(char forcechain, ofstream &stream);
 		double	CalCaRmsd(PROT *pro, int resn, int *res1, int *res2);
diff --git a/FATCATMain/basic.h b/FATCATMain/basic.h
index b8739a6..e63276c 100644
--- a/FATCATMain/basic.h
+++ b/FATCATMain/basic.h
@@ -5,6 +5,8 @@
 #include <sstream>
 #include <iosfwd>
 #include <string>
+#include <cstring>
+#include <cstdlib>
 #include <vector>
 #include <unistd.h>
 #include <math.h>
diff --git a/FATCATMain/basic_90.h b/FATCATMain/basic_90.h
index b8739a6..e63276c 100644
--- a/FATCATMain/basic_90.h
+++ b/FATCATMain/basic_90.h
@@ -5,6 +5,8 @@
 #include <sstream>
 #include <iosfwd>
 #include <string>
+#include <cstring>
+#include <cstdlib>
 #include <vector>
 #include <unistd.h>
 #include <math.h>
